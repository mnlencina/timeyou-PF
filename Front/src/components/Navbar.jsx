import React from "react";
import styled from "styled-components";

export const Navbar = () => {
  return (
    <Container>
      <h1> Navbar</h1>
    </Container>
  );
};

const Container = styled.div``;
